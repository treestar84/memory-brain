{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -d \"$CLAUDE_PLUGIN_ROOT/node_modules/yaml\" && exit 0; echo '[memory-brain] Dependencies not installed yet. Run /memory-brain:setup once (bun install, with your confirmation).'"
          }
        ]
      }
    ]
  }
}
