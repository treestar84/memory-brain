{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v cfgm >/dev/null 2>&1 && exit 0; echo '[memory-brain] cfgm is not on PATH yet. Run /memory-brain:setup once to provision it (bun install + bun link, with your confirmation).'"
          }
        ]
      }
    ]
  }
}
