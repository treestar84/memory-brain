{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v cfgm >/dev/null 2>&1 || (cd \"${CLAUDE_PLUGIN_ROOT}\" && timeout 60 bun install --silent && timeout 60 bun link) >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
